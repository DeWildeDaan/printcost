apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-printcost
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-printcost
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
